     1→     1→FILE:   src/app/api/onboarding/create-wedding/route.ts:365-388 (15 keys as string literals)
     2→     2→        src/components/Footer.tsx:33-42 (reads s.bride_name, s.groom_name, s.hashtag, s.site_subtitle, s.couple_photo_1, s.couple_photo_2)
     3→     3→        src/components/GuestSearch.tsx:95-97 (reads s.bride_name, s.groom_name)
     4→     4→        src/components/HeroSection.tsx (similar)
     5→     5→PROOF:  The string `'bride_name'` appears as a literal in 4+ files. There's no `SETTING_KEYS`
     6→     6→        enum. A typo like `brideName` instead of `bride_name` would silently fall back to default.
     7→     7→IMPACT: Hard to refactor; typo bugs are silent; no autocomplete in IDE.
     8→     8→FIX:    Add src/lib/settings-keys.ts exporting `SETTING_KEYS = { BRIDE_NAME: 'bride_name', GROOM_NAME: 'groom_name', ... } as const`.
     9→     9→        Use it in the 4+ sites.
    10→    10→EFFORT: S
    11→    11→```
    12→    12→
    13→    13→```
    14→    14→P2-CQ-10: createWedding POST handler is 453 lines (lines 93-545)
    15→    15→FILE:   src/app/api/onboarding/create-wedding/route.ts:93-545
    16→    16→PROOF:  Single exported function `POST` spans 453 lines. Mixes: auth check, body validation
    17→    17→        (15 fields), uniqueness checks (3 tables), a 200-line transaction body, post-tx cache
    18→    18→        invalidation, WhatsApp message building. The transaction body alone (lines 320-506)
    19→    19→        is 186 lines.
    20→    20→IMPACT: Hard to test (can't mock individual steps). Hard to review (one PR diff touches 450
    21→    21→        lines). Hard to extend (a new step like "send welcome email" goes inside the tx).
    22→    22→        **DUPLICATE of P1-CQ-7 (open)** — listed here for the L-effort follow-up.
    23→    23→FIX:    Extract: (1) `validateCreateWeddingBody(body): Result<ValidatedBody, ApiError>` in
    24→    24→        src/lib/onboarding/validate.ts. (2) `createWeddingInTx(tx, validated, user): Result`
    25→    25→        in src/lib/onboarding/create-wedding-tx.ts. (3) `buildWhatsAppPayload(...)` (already
    26→    26→        in lib/billing.ts). Route handler shrinks to ~60 lines.
    27→    27→EFFORT: L
    28→    28→```
    29→    29→
    30→    30→```
    31→    31→P2-CQ-11: duplicateWedding POST handler is 200 lines (lines 29-234)
    32→    32→FILE:   src/app/api/platform/weddings/[id]/duplicate/route.ts:29-234
    33→    33→PROOF:  Single function. 11 numbered sections (fetch source, validate, uniqueness check,
    34→    34→        create wedding, copy settings, copy theme, copy music, copy timeline, copy stories,
    35→    35→        audit log, cache invalidate). Each "copy" section is 5-15 lines of mapping.
    36→    36→IMPACT: Same as P2-CQ-10. Also: copy logic is inline — adding a new duplicable relation
    37→    37→        (e.g. WeddingCollectionBinding) requires editing this 200-line function.
    38→    38→        **DUPLICATE of P1-CQ-7 (open)**.
    39→    39→FIX:    Extract each "copy" section into a helper: `copySettings(tx, sourceId, targetId)`,
    40→    40→        `copyTheme(...)`, etc. Route handler shrinks to ~80 lines.
    41→    41→EFFORT: M
    42→    42→```
    43→    43→
    44→    44→```
    45→    45→P2-CQ-12: 4 admin-UI components exceed 500 lines (GuestManager 1099, BillingTab 1202, OnboardingTab 2150, page.tsx 2452)
    46→    46→FILE:   src/app/platform/admin/page.tsx (2452 lines, 5 inline tab components)
    47→    47→        src/app/platform/admin/OnboardingTab.tsx (2150 lines, OnboardingTab function 1026 lines)
    48→    48→        src/app/platform/admin/BillingTab.tsx (1202 lines, BillingTab function ~880 lines)
    49→    49→        src/components/admin/GuestManager.tsx (1099 lines, GuestManager function 952 lines)
    50→    50→PROOF:  `wc -l` on the 4 files. P1-CQ-8 listed these; P1-UX-9 covered the code-splitting angle.
    51→    51→        The functions inside still mix state, fetch, and rendering in one body.
    52→    52→        **DUPLICATE of P1-CQ-8 + P1-UX-9 (open, L-effort)**.
    53→    53→IMPACT: Re-render storms (any state change re-renders 1000+ lines), impossible to memoize
    54→    54→        sub-sections, slow first paint (large JS bundle), hard to onboard new devs.
    55→    55→FIX:    Extract each tab into its own file (already partially done — OnboardingTab, BillingTab,
    56→    56→        CollectionsFactoryTab exist). Inside each, extract the wizard steps / form sections /
    57→    57→        table rows into sub-components. Target: each file ≤ 500 lines, each function ≤ 100.
    58→    58→EFFORT: L
    59→    59→```
    60→    60→
    61→    61→```
    62→    62→P2-CQ-13: platform/admin/page.tsx duplicates WEDDING_STATUSES + PLANS + STATUS_LABELS already in lib/
    63→    63→FILE:   src/app/platform/admin/page.tsx:236  `const WEDDING_STATUSES: WeddingStatus[] = [...]`
    64→    64→        src/app/platform/admin/page.tsx:237  `const PLANS: Plan[] = [...]`
    65→    65→        src/app/platform/admin/page.tsx:239-253  STATUS_LABELS + STATUS_BADGE_CLASS
    66→    66→        src/app/platform/admin/page.tsx:255-260  PLAN_BADGE_CLASS
    67→    67→        src/app/platform/admin/page.tsx:262-268  ROLE_BADGE_CLASS
    68→    68→PROOF:  VALID_STATUSES is already in src/lib/wedding-status.ts (imported on line 26 of
    69→    69→        platform/weddings/route.ts). VALID_PLANS is in src/lib/constants.ts. The UI redefines
    70→    70→        them locally + adds badge CSS classes that don't exist anywhere else.
    71→    71→IMPACT: Drift between API-side status list and UI-side list. Adding a new status (e.g.
    72→    72→        'CANCELLED') requires touching 5+ files.
    73→    73→FIX:    Add src/lib/ui-labels.ts exporting STATUS_LABELS, STATUS_BADGE_CLASS, PLAN_BADGE_CLASS,
    74→    74→        ROLE_BADGE_CLASS, ROLE_LABELS. Import in admin page + sub-tabs.
    75→    75→EFFORT: S
    76→    76→```
    77→    77→
    78→    78→```
    79→    79→P2-CQ-14: ROLE_LABELS duplicated between src/lib/auth.ts and src/app/platform/admin/page.tsx
    80→    80→FILE:   src/lib/auth.ts:284-290  ROLE_LABELS Record (server-side, used by getRoleLabel)
    81→    81→        src/app/platform/admin/page.tsx:102-108  ROLE_LABELS Record (client-side copy)
    82→    82→PROOF:  The platform/admin page.tsx file has a comment at line 99: "Local role labels — we
    83→    83→        can't import from @/lib/auth because that module imports `next/headers` + Prisma
    84→    84→        (server-only)." So the team duplicated the const rather than extracting to a shared
    85→    85→        client-safe module.
    86→    86→IMPACT: Drift risk + the comment itself is a code smell — the auth module is mixing server-only
    87→    87→        concerns (Prisma, next/headers) with pure data (ROLE_LABELS).
    88→    88→FIX:    Move ROLE_LABELS + getRoleLabel to src/lib/types.ts (pure, no Prisma import). Both
    89→    89→        server and client can import from there. Remove the local copy in page.tsx.
    90→    90→EFFORT: S
    91→    91→```
    92→    92→
    93→    93→```
    94→    94→P2-CQ-15: Dead code — /api/route.ts returns "Hello, world!" with force-dynamic
    95→    95→FILE:   src/app/api/route.ts (6 lines)
    96→    96→PROOF:  ```ts
    97→    97→        export const dynamic = "force-dynamic";
    98→    98→        export async function GET() {
    99→    99→          return NextResponse.json({ message: "Hello, world!" });
   100→   100→        }
   101→   101→        ```
   102→   102→        This is the default Next.js scaffolding stub. No client fetches /api. The force-dynamic
   103→   103→        export means even this trivial response is rendered per-request.
   104→   104→IMPACT: Confusing for new devs (looks like an intentional health check). Tiny perf cost.
   105→   105→        **DUPLICATE of an item noted in P1-AUDIT-PROD verdict** ("non-P1 observation").
   106→   106→FIX:    Delete the file. Or: replace with a redirect to /api/health for discoverability.
   107→   107→EFFORT: S
   108→   108→```
   109→   109→
   110→   110→```
   111→   111→P2-CQ-16: Dashboard route runs 16 parallel Prisma queries (Promise.all of 16 array items)
   112→   112→FILE:   src/app/api/platform/dashboard/route.ts:85-189
   113→   113→PROOF:  The destructuring at line 85-102 lists 16 queries: weddingsTotal, weddingsByStatus,
   114→   114→        weddingsByPlan, usersTotal, platformAdminCount, usersByRole, guestsTotal, guestsLast7Days,
   115→   115→        recentWeddings, recentActivity, publishedWeddingsForMrr, weddingsCreatedSince6Mo,
   116→   116→        suspended30d, archived30d, newWeddings30d, newGuests30d.
   117→   117→IMPACT: Function is hard to read (16-element destructuring). One query failure (e.g.
   118→   118→        recentActivity's `include.user` fails on a deleted user) 500s the entire dashboard.
   119→   119→        Hard to add a new metric without touching the destructuring.
   120→   120→FIX:    Group queries into logical chunks (e.g. `const weddingStats = await getWeddingStats()`,
   121→   121→        `const revenueStats = await getRevenueStats()`). Each chunk is a separate function
   122→   122→        returning a typed object. Route handler composes them.
   123→   123→EFFORT: M
   124→   124→```
   125→   125→
   126→   126→```
   127→   127→P2-CQ-17: guest/access-logs/route.ts runs 14+ parallel counts + 2 findMany + 1 groupBy
   128→   128→FILE:   src/app/api/guest/access-logs/route.ts:32-99
   129→   129→PROOF:  Line 32-47: Promise.all of [logs.findMany, logs.count]. Line 49-69: Promise.all of 14
   130→   130→        count queries (totalLogins, totalAccessDenied, ..., activeSessions). Line 72-77:
   131→   131→        recentAccessDenied count. Line 79-82: failedAttempts findMany. Line 96-99:
   132→   132→        categoryBreakdown groupBy. Total: 19 Prisma queries per request.
   133→   133→IMPACT: Same as P2-CQ-16. Plus: most of the 14 counts could be a single `groupBy({ by: ['action'],
   134→   134→        _count: { _all: true } })` returning all action counts in one query. The current
   135→   135→        implementation runs 14 round-trips for data that's 1 groupBy away.
   136→   136→FIX:    Replace the 14 count queries with 1 groupBy. Replace the failedAttempts findMany +
   137→   137→        in-JS reduce with a Prisma `groupBy({ by: ['ipAddress'], _count: ..., where: { action: { in: [...] } } })`.
   138→   138→        Route shrinks from 120 to ~50 lines.
   139→   139→EFFORT: M
   140→   140→```
   141→   141→
   142→   142→```
   143→   143→P2-CQ-18: Missing JSDoc on most exported API route handlers
   144→   144→FILE:   src/app/api/guests/route.ts (0 JSDoc on GET/POST/PUT/DELETE)
   145→   145→        src/app/api/guests/[id]/route.ts (0 JSDoc)
   146→   146→        src/app/api/tables/route.ts (0 JSDoc)
   147→   147→        src/app/api/couple-story/route.ts (0 JSDoc)
   148→   148→        src/app/api/timeline/route.ts (0 JSDoc)
   149→   149→        src/app/api/media/route.ts (0 JSDoc)
   150→   150→        src/app/api/settings/route.ts (0 JSDoc)
   151→   151→        src/app/api/theme/route.ts (0 JSDoc)
   152→   152→        src/app/api/admin/users/route.ts (only inline // comments, no JSDoc)
   153→   153→        src/app/api/guest/auth/route.ts (0 JSDoc)
   154→   154→        (and ~25 more)
   155→   155→PROOF:  `rg "/\*\*" src/app/api --type ts | wc -l` returns ~12 hits — only 6 routes have
   156→   156→        JSDoc on their handlers (onboarding/create-wedding, onboarding/leads, platform/weddings,
   157→   157→        platform/weddings/[id], platform/dashboard, platform/users, guest/lookup, health,
   158→   158→        collections/deploy). The other ~44 routes have no documentation.
   159→   159→IMPACT: New devs must read the implementation to learn what an endpoint does. No auto-generated
   160→   160→        API reference possible.
   161→   161→FIX:    Add a one-paragraph JSDoc to each exported GET/POST/PUT/DELETE. Template:
   162→   162→        `/api/{resource} — {auth} — {what it does} — {request shape} — {response shape}`.
   163→   163→EFFORT: M (mechanical, ~44 routes × 1-2 paragraphs each)
   164→   164→```
   165→   165→
   166→   166→```
   167→   167→P2-CQ-19: usePlatformFetch hook duplicates fetch-with-auth pattern that exists in 3 other admin pages
   168→   168→FILE:   src/app/platform/admin/page.tsx:291-347  usePlatformFetch
   169→   169→        src/app/w/[slug]/admin/page.tsx (similar inline pattern, lines 50-100 approx)
   170→   170→        src/components/admin/LoginForm.tsx (similar)
   171→   171→        src/components/admin/GuestManager.tsx (similar)
   172→   172→PROOF:  Each admin client component re-implements: read token from localStorage, attach
   173→   173→        `Authorization: Bearer ${token}`, handle 401 → redirect to login, handle 403 → toast,
   174→   174→        handle network error → toast. The implementations diverge slightly (some use refs,
   175→   175→        some use state, some use try/catch, some use .catch).
   176→   176→IMPACT: Drift in error handling UX. Bug fixes (e.g. handling 429) must be applied in 4 places.
   177→   177→FIX:    Extract `useAuthedFetch()` hook to src/hooks/use-authed-fetch.ts. All 4 sites import it.
   178→   178→EFFORT: M
   179→   179→```
   180→   180→
   181→   181→```
   182→   182→P2-CQ-20: TODO/FIXME-style comments embedded in long inline comments (no formal markers)
   183→   183→FILE:   src/app/api/onboarding/create-wedding/route.ts:341-345  ("Without these rows, a freshly onboarded wedding would show the default couple's names until the organizer logs in")
   184→   184→        src/app/api/platform/weddings/[id]/duplicate/route.ts:161-164  ("re-using the URL is safe + storage-efficient")
   185→   185→        src/app/api/guest/auth/route.ts (no inline TODOs, but the file's structure is hard to follow)
   186→   186→PROOF:  The codebase has 0 formal `TODO/FIXME/HACK/XXX` markers (verified by P1-AUDIT-CQ).
   187→   187→        But it has many long inline comments that explain design decisions — these are good
   188→   188→        documentation but not actionable debt markers. The pattern works today but as the
   189→   189→        codebase grows, design-decision comments will accumulate and obscure the code.
   190→   190→IMPACT: Low — current state is fine. Listed as P2-CQ for awareness, no action needed unless
   191→   191→        the team adopts a `// DESIGN:` or `// NOTE:` convention.
   192→   192→FIX:    (optional) Adopt a `// NOTE:` prefix for design-decision comments, leaving `// TODO:`
   193→   193→        for action items. Then a future grep can separate the two.
   194→   194→EFFORT: S (convention-only)
   195→   195→```
   196→   196→
   197→   197→```
   198→   198→P2-CQ-21: setAuthCookie helper exists but is used in only 1 of 4 cookie-setting sites
   199→   199→FILE:   src/lib/auth.ts:263-272  setAuthCookie(response, token) helper
   200→   200→        Used in: src/app/api/admin/login/route.ts:82 ✓
   201→   201→        NOT used in: src/app/api/platform/login/route.ts (sets cookie inline)
   202→   202→        NOT used in: src/app/api/guest/auth/route.ts:153-161 (sets guest_session cookie inline)
   203→   203→        NOT used in: src/app/api/guest/auto-auth/route.ts:149-153 (inline)
   204→   204→        NOT used in: src/app/api/guest/invite/route.ts:125-129 (inline)
   205→   205→PROOF:  `rg -n "setAuthCookie" src/` returns 2 hits (definition + 1 call site). The other
   206→   206→        4 cookie-setting sites manually call `response.cookies.set({...})` with slightly
   207→   207→        different options (different maxAge values, different sameSite).
   208→   208→IMPACT: Same cookie options drift as P2-SEC-4. The helper exists but isn't used.
   209→   209→FIX:    Add a `setGuestSessionCookie(response, token)` helper alongside setAuthCookie. Use
   210→   210→        both in all 5 sites. Standardize cookie options.
   211→   211→EFFORT: S
   212→   212→```
   213→   213→
   214→   214→```
   215→   215→P2-CQ-22: dashboard route couples NRR/MRR/churn/growth analytics in one 314-line handler
   216→   216→FILE:   src/app/api/platform/dashboard/route.ts:67-313 (full GET handler)
   217→   217→PROOF:  Single function computes: weddings by status/plan, users by role, guests total/7d,
   218→   218→        recent weddings (5), recent activity (20 audit logs), MRR + ARPU + byPlan + 6-mo series,
   219→   219→        churn (30d suspended + archived), growth (30d + 6-mo series). Each "section" is
   220→   220→        20-40 lines of fetch + transform.
   221→   221→IMPACT: Hard to test (one handler = one test case). Hard to evolve (e.g. add LTV metric
   222→   222→        requires touching the 250-line function). Hard to parallelize (all queries in one
   223→   223→        Promise.all — can't selectively cache the slow ones).
   224→   224→FIX:    Extract `getWeddingStats()`, `getUserStats()`, `getGuestStats()`, `getRevenueStats()`,
   225→   225→        `getChurnStats()`, `getGrowthStats()` — each returning a typed object. Compose in the
   226→   226→        route handler.
   227→   227→EFFORT: M
   228→   228→```
   229→   229→
   230→   230→#### P2-PERF — Performance Issues
   231→   231→
   232→   232→```
   233→   233→P2-PERF-1: N+1 query in /api/guests/import-docx — up to 10 findFirst per guest for invitation-code uniqueness
   234→   234→FILE:   src/app/api/guests/import-docx/route.ts:278-389
   235→   235→PROOF:  Outer `for (const parsedTable of parsedTables)` loop (line 278) iterates tables.
   236→   236→          Inner `for (const parsedGuest of parsedTable.guests)` loop (line 314) iterates guests.
   237→   237→            Inside: `tenantDb.table.findFirst` (line 281) — 1 query per table
   238→   238→            Inside: `tenantDb.guest.findFirst` (line 317) for duplicate check — 1 query per guest
   239→   239→            Inside: `do { ... tenantDb.guest.findFirst({ where: { invitationCode } }) ... } while (codeAttempts < 10)`
   240→   240→              (lines 339-347) — up to 10 queries per guest for invitation code uniqueness!
   241→   241→            Inside: `tenantDb.guest.create` (line 360) — 1 insert per guest
   242→   242→        For a 200-guest import: 200 tables-lookups + 200 dup-checks + ~200 avg 5 code-uniqueness-checks + 200 inserts = ~1000 queries.
   243→   243→IMPACT: A 200-guest DOCX import takes 5-30 seconds (vs <500ms with createMany). SQLite
   244→   244→        single-writer lock means the platform stalls for the duration. Frontend times out.
   245→   245→FIX:    (1) Generate invitation codes in a single batch upfront (200 codes via `Array.from({length: 200}, () => uuid().substring(0,6).toUpperCase())`)
   246→   246→        then deduplicate against a single `findMany({ where: { invitationCode: { in: codes } } })` query.
   247→   247→        (2) Use `tenantDb.guest.createMany({ data: [...] })` for the inserts.
   248→   248→        (3) Use `tenantDb.table.findMany({ where: { number: { in: [...] } } })` to batch the table lookups.
   249→   249→        Target: 4-6 queries total regardless of guest count.
   250→   250→EFFORT: M
   251→   251→```
   252→   252→
   253→   253→```
   254→   254→P2-PERF-2: N+1 query in /api/guests/import (XLSX) — sequential create per row
   255→   255→FILE:   src/app/api/guests/import/route.ts:41-79
   256→   256→PROOF:  `for (let i = 0; i < rows.length; i++) { ... await tenantDb.guest.create({ data: ... }) ... }`
   257→   257→        — one create per row, no batching. Also generates a new invitationCode per row but
   258→   258→        doesn't check for collisions (relies on the schema's @@unique constraint to throw,
   259→   259→        caught by the per-row try/catch — so a collision = silent row skip).
   260→   260→IMPACT: 200-row import = 200 sequential inserts ≈ 2-5 seconds. SQLite lock held throughout.
   261→   261→FIX:    Pre-generate codes in batch, dedupe via 1 findMany, then `createMany({ data: [...], skipDuplicates: true })`.
   262→   262→        Return `{ imported, skipped, errors }` in one shot.
   263→   263→EFFORT: S
   264→   264→```
   265→   265→
   266→   266→```
   267→   267→P2-PERF-3: Missing pagination on /api/guests/export — fetches ALL guests into memory
   268→   268→FILE:   src/app/api/guests/export/route.ts:22-25
   269→   269→PROOF:  `const guests = await tenantDb.guest.findMany({ include: { table: { select: { name, number } } }, orderBy: { createdAt: 'desc' } })`
   270→   270→        — no `take` limit. For a 1000-guest wedding this fetches 1000 rows + 1000 table joins
   271→   271→        into a single JS array, then maps them all to XLSX rows in memory, then builds an
   272→   272→        XLSX buffer.
   273→   273→IMPACT: Memory spike on large weddings. XLSX buffer for 1000 rows ≈ 5-10MB. Under concurrent
   274→   274→        exports (multiple admins), Node heap pressure.
   275→   275→FIX:    (a) Stream the response using XLSX's stream API (xlsx-streambook or exceljs). OR
   276→   276→        (b) Add a 5000-row hard cap with a 406 error above. OR (c) Document the limit and
   277→   277→        add a progress bar on the client.
   278→   278→EFFORT: M
   279→   279→```
   280→   280→
   281→   281→```
   282→   282→P2-PERF-4: Missing pagination on 3 public list endpoints (couple-story, tables, timeline)
   283→   283→FILE:   src/app/api/couple-story/route.ts:9-13  `findMany({ orderBy: { order: 'asc' } })` — no take
   284→   284→        src/app/api/tables/route.ts:17-21       `findMany({ include: { _count: ... }, orderBy: { number: 'asc' } })` — no take
   285→   285→        src/app/api/timeline/route.ts:10-13     `findMany({ orderBy: { order: 'asc' } })` — no take
   286→   286→PROOF:  All 3 return the full collection. Tables and timeline are bounded by the plan's
   287→   287→        guest/table limits, so they're small. CoupleStory has no limit — a couple could
   288→   288→        upload 100+ stories (there's no UI limit either).
   289→   289→IMPACT: Low for tables/timeline (plan-limited). Medium for couple-story (no limit). The
   290→   290→        real risk is the public homepage fetching all 3 endpoints in parallel on every page load.
   291→   291→FIX:    Add `take: 50` to couple-story. Tables and timeline are bounded by plan limits —
   292→   292→        leave as-is but add a `take: 200` safety cap.
   293→   293→EFFORT: S
   294→   294→```
   295→   295→
   296→   296→```
   297→   297→P2-PERF-5: bcrypt hash inside db.$transaction (extends lock duration)
   298→   298→FILE:   src/app/api/onboarding/create-wedding/route.ts:399
   299→   299→PROOF:  (see P2-SEC-10 for full proof) — bcrypt(rounds=12) ≈ 200-300ms inside the tx.
   300→   300→IMPACT: Under load, concurrent onboarding calls serialize on the bcrypt step. SQLite
   301→   301→        single-writer lock means ALL writes platform-wide stall for the duration.
   302→   302→        **DUPLICATE of P2-SEC-10** (perf angle).
   303→   303→FIX:    Move `hashPassword(organizerPassword)` above the `db.$transaction()` call.
   304→   304→EFFORT: S
   305→   305→```
   306→   306→
   307→   307→```
   308→   308→P2-PERF-6: Platform dashboard fetches ALL published weddings to compute MRR in JS
   309→   309→FILE:   src/app/api/platform/dashboard/route.ts:160-163
   310→   310→PROOF:  `db.wedding.findMany({ where: { status: 'PUBLISHED' }, select: { createdAt: true, plan: true } })`
   311→   311→        — fetches every published wedding's createdAt + plan into a JS array, then in
   312→   312→        `mrrSeries` (line 237-251) filters and reduces the array 6 times (once per month).
   313→   313→        For 1000 published weddings: 1000-row fetch + 6000-iteration reduce.
   314→   314→IMPACT: Dashboard GET latency grows linearly with platform success. At 10k weddings, this
   315→   315→        is the slowest query in the API. The 6-month MRR series is the worst — 6 passes
   316→   316→        over the full array.
   317→   317→FIX:    Replace with a single `db.wedding.groupBy({ by: ['plan'], where: { status: 'PUBLISHED' }, _count: { _all: true } })`
   318→   318→        for the current MRR. For the series, add a `publishedAt` index + use
   319→   319→        `db.wedding.groupBy({ by: ['plan', <date_trunc>], ... })` — SQLite doesn't support
   320→   320→        date_trunc, so a `findMany({ select: { createdAt: true, plan: true }, where: { createdAt: { gte: sixMonthsAgo } } })`
   321→   321→        + bucketing in JS is acceptable for the series (the array is already scoped to 6 months).
   322→   322→EFFORT: S
   323→   323→```
   324→   324→
   325→   325→```
   326→   326→P2-PERF-7: Admin dashboard route runs 7 Promise.all queries + 1 separate recentActivity query
   327→   327→FILE:   src/app/api/admin/dashboard/route.ts:21-44
   328→   328→PROOF:  Line 21-32: Promise.all of 7 queries (5 guest counts by status, 1 table count, 1
   329→   329→        table findMany). Line 39-44: separate `db.auditLog.findMany` (NOT in the Promise.all).
   330→   330→        Line 46-49: separate `tenantDb.guest.groupBy` for categoryStats.
   331→   331→        Total: 9 sequential round-trips even though 7 are parallelized.
   332→   332→IMPACT: Per-wedding admin dashboard takes 3× the optimal latency (3 sequential groups
   333→   333→        instead of 1 Promise.all).
   334→   334→FIX:    Move recentActivity + categoryStats into the Promise.all. All 9 queries run in
   335→   335→        parallel. Latency drops from ~3 round-trips to ~1.
   336→   336→EFFORT: S
   337→   337→```
   338→   338→
   339→   339→```
   340→   340→P2-PERF-8: Guest access-logs route runs 19 Prisma queries per request
   341→   341→FILE:   src/app/api/guest/access-logs/route.ts (see P2-CQ-17 for full proof)
   342→   342→PROOF:  14 parallel count queries (line 49-69) that could be a single groupBy, plus a
   343→   343→        findMany for failedAttempts (line 79-82) that could be a groupBy by ipAddress.
   344→   344→IMPACT: 19 queries per page load of the access-logs tab. For a wedding with 10k access
   345→   345→        log rows, the 14 counts each scan the full table.
   346→   346→        **DUPLICATE of P2-CQ-17** (perf angle).
   347→   347→FIX:    Replace the 14 counts with 1 groupBy({ by: ['action'], _count: { _all: true } }).
   348→   348→        Replace failedAttempts findMany + reduce with groupBy({ by: ['ipAddress'] }).
   349→   349→        19 queries → 4 queries.
   350→   350→EFFORT: M
   351→   351→```
   352→   352→
   353→   353→```
   354→   354→P2-PERF-9: /api/route.ts uses force-dynamic for a static "Hello, world!" response
   355→   355→FILE:   src/app/api/route.ts:1-6
   356→   356→PROOF:  `export const dynamic = "force-dynamic";` + returns a constant JSON. No DB, no
   357→   357→        request-dependent data. Should be cached at the edge.
   358→   358→IMPACT: Every request to /api renders the response from scratch (negligible cost, but
   359→   359→        pointless). Also: the endpoint serves no purpose (see P2-CQ-15).
   360→   360→FIX:    Delete the file (preferred) or remove the `force-dynamic` export to let Next.js
   361→   361→        cache it.
   362→   362→EFFORT: S
   363→   363→```
   364→   364→
   365→   365→```
   366→   366→P2-PERF-10: All 50 API routes use force-dynamic — some could be cached or static
   367→   367→FILE:   src/app/api/**/route.ts (50 files all export `force-dynamic`)
   368→   368→PROOF:  `rg -l "force-dynamic" src/app/api | wc -l` returns 50. Some routes are inherently
   369→   369→        dynamic (auth, write endpoints), but read-only public endpoints like:
   370→   370→          - GET /api/couple-story (per-wedding, changes rarely)
   371→   371→          - GET /api/timeline (per-wedding, changes rarely)
   372→   372→          - GET /api/settings (per-wedding, changes rarely)
   373→   373→          - GET /api/theme (per-wedding, changes rarely)
   374→   374→        Could use `revalidate = 60` (ISR) instead of force-dynamic, caching per-wedding
   375→   375→        responses for 60s. The weddingCache in tenant-context.ts already caches the wedding
   376→   376→        lookup; the response itself is re-computed every time.
   377→   377→IMPACT: Every public page load hits 4-5 API endpoints that all re-run Prisma queries. With
   378→   378→        ISR, the second+ visitor within 60s gets a cached response. ~30-50% reduction in DB
   379→   379→        load for popular weddings.
   380→   380→FIX:    Replace `export const dynamic = 'force-dynamic'` with `export const revalidate = 60`
   381→   381→        in the 4 read-only public routes above. Invalidate via `revalidatePath('/w/[slug]')`
   382→   382→        or `revalidateTag('wedding-' + slug)` when settings/theme/timeline change.
   383→   383→EFFORT: M (requires invalidation logic in the PUT routes)
   384→   384→```
   385→   385→
   386→   386→```
   387→   387→P2-PERF-11: Missing index on Guest.invitationCode (queried on every guest auth)
   388→   388→FILE:   prisma/schema.prisma:199  `invitationCode String` — no @@index
   389→   389→        schema has @@unique([weddingId, invitationCode]) at line 217 which creates a
   390→   390→        composite index, but `findFirst({ where: { invitationCode } })` queries don't benefit
   391→   391→        from it because they don't filter by weddingId first.
   392→   392→PROOF:  `tenantDb.guest.findFirst({ where: { invitationCode } })` is used in:
   393→   393→          - src/app/api/guest/auth/route.ts:114
   394→   394→          - src/app/api/guest/invite/route.ts:54
   395→   395→          - src/app/api/guests/qrcode/[code]/route.ts:44
   396→   396→        The tenant extension auto-injects weddingId, so the actual query becomes
   397→   397→        `findFirst({ where: { invitationCode, weddingId: '<ctx>' } })` which DOES use the
   398→   398→        composite unique index. So this is NOT actually an issue — the extension saves us.
   399→   399→        Listed here because a developer reading the route file in isolation might think it's
   400→   400→        an unindexed lookup.
   401→   401→IMPACT: None — the tenant-scoped extension's auto-injection makes the composite index usable.
   402→   402→        **NO ACTION NEEDED** (documented for awareness).
   403→   403→FIX:    Add a code comment in the 3 sites: "// tenant extension auto-injects weddingId → uses @@unique([weddingId, invitationCode]) index"
   404→   404→EFFORT: S
   405→   405→```
   406→   406→
   407→   407→```
   408→   408→P2-PERF-12: Missing index on AdminUser.email lookups during login
   409→   409→FILE:   prisma/schema.prisma:71  `email String @unique`
   410→   410→PROOF:  The @unique constraint at line 71 automatically creates a unique index. So
   411→   411→        `findUnique({ where: { email } })` is indexed. NOT an issue.
   412→   412→IMPACT: None. **NO ACTION NEEDED**.
   413→   413→EFFORT: -
   414→   414→```
   415→   415→
   416→   416→```
   417→   417→P2-PERF-13: Platform admin page eagerly imports 7 large tab components + PenpotStudio
   418→   418→FILE:   src/app/platform/admin/page.tsx:93-97
   419→   419→PROOF:  ```ts
   420→   420→        import { BillingTab } from './BillingTab'         // 1202 lines
   421→   421→        import { OnboardingTab } from './OnboardingTab'   // 2150 lines
   422→   422→        import { CollectionsFactoryTab } from './CollectionsFactoryTab'  // 315 lines
   423→   423→        import { ThemeCustomizer } from '@/components/admin/ThemeCustomizer'  // 615 lines
   424→   424→        import { PenpotStudio } from '@/components/penpot/PenpotStudio'      // unknown size
   425→   425→        ```
   426→   426→        All 5 are statically imported. The initial JS bundle for /platform/admin includes
   427→   427→        ALL of them, even though only 1 tab is visible at a time. PenpotStudio (a canvas
   428→   428→        editor) loads even if the user never opens the "Studio" tab.
   429→   429→IMPACT: Estimated 200-400KB of JS shipped on first paint of /platform/admin. Slow on 3G.
   430→   430→        **DUPLICATE of P1-UX-9 (open, M-effort)**.
   431→   431→FIX:    `const PenpotStudio = dynamic(() => import(...).then(m => m.PenpotStudio), { ssr: false })`
   432→   432→        Same for ThemeCustomizer, CollectionsFactoryTab, BillingTab, OnboardingTab.
   433→   433→EFFORT: M
   434→   434→```
   435→   435→
   436→   436→```
   437→   437→P2-PERF-14: No cursor-based pagination anywhere — all list endpoints use offset (skip+take)
   438→   438→FILE:   src/app/api/guests/route.ts:51  `skip, take: limit` (offset pagination)
   439→   439→        src/app/api/platform/weddings/route.ts:89-90  (same)
   440→   440→        src/app/api/platform/users/route.ts:68-69  (same)
   441→   441→        src/app/api/onboarding/leads/route.ts:245-246  (same)
   442→   442→        src/app/api/guest/access-logs/route.ts:36  (same)
   443→   443→PROOF:  All 5 paginated list endpoints use `skip = (page - 1) * limit; take = limit`.
   444→   444→        For deep pages (e.g. page 100 of 1000 guests), SQLite still scans the first 9900 rows.
   445→   445→IMPACT: Deep pagination latency. Low for current scale (most weddings < 500 guests) but
   446→   446→        will degrade as the platform grows.
   447→   447→FIX:    For guests (the largest lists), add cursor pagination: `?cursor=<createdAt>&take=20`
   448→   448→        with `where: { createdAt: { lt: cursor } }`. UI changes to "Load more" instead of
   449→   449→        page numbers. Offset pagination is fine for admin tables where users rarely go past
   450→   450→        page 5.
   451→   451→EFFORT: M (guests only — others can stay offset)
   452→   452→```
   453→   453→
   454→   454→```
   455→   455→P2-PERF-15: Module-scope setInterval in lib/guest-auth.ts:202 and auto-auth/route.ts:17
   456→   456→FILE:   src/lib/guest-auth.ts:202  `setInterval(() => { ... }, 10 * 60 * 1000)`
   457→   457→        src/app/api/guest/auto-auth/route.ts:17  `setInterval(() => { usedLookupTokens.clear(); }, 10 * 60 * 1000)`
   458→   458→PROOF:  Both are module-scope, never cleared. P1-CQ-20 was supposed to move them to
   459→   459→        instrumentation.ts. The worklog P1-LAUNCH summary lists P1-CQ-1-20 as "remaining
   460→   460→        P1" so this is still open.
   461→   461→IMPACT: In development with HMR, each module reload adds another setInterval. After 10
   462→   462→        reloads, 10 intervals run. In production: prevents graceful shutdown (the interval
   463→   463→        keeps the event loop alive past SIGTERM until Docker SIGKILLs at 10s).
   464→   464→        **DUPLICATE of P1-CQ-20 (open, S-effort)**.
   465→   465→FIX:    Move both setInterval calls into `src/instrumentation.ts register()` (which P1-PROD-7
   466→   466→        added). Store the handles and `clearInterval()` on SIGTERM.
   467→   467→EFFORT: S
   468→   468→```
   469→   469→
   470→   470→```
   471→   471→P2-PERF-16: Missing Suspense boundaries around slow data fetches
   472→   472→FILE:   src/app/page.tsx (root) — wraps GuestAuthProvider in Suspense but not the data fetches
   473→   473→        src/app/w/[slug]/page.tsx — no Suspense around fetch calls
   474→   474→PROOF:  P1-UX-1 added loading.tsx + error.tsx at the route-segment level. But individual
   475→   475→        data fetches inside pages (e.g. `fetch('/api/settings')` in Footer, GuestSearch,
   476→   476→        HeroSection) are not wrapped in Suspense. They show empty state until the fetch
   477→   477→        resolves.
   478→   478→IMPACT: Layout shift + empty sections on first paint. Not a P1 (loading.tsx covers route
   479→   479→        transitions) but a polish issue.
   480→   480→        **DUPLICATE of P1-UX-1 (partially addressed)**.
   481→   481→FIX:    Wrap each async data fetch in `<Suspense fallback={<Skeleton/>}>`. Or convert the
   482→   482→        fetches to React Server Components where possible (Footer is a candidate — it's
   483→   483→        currently a client component just for the settings fetch).
   484→   484→EFFORT: M
   485→   485→```
   486→   486→
   487→   487→```
   488→   488→P2-PERF-17: Footer, GuestSearch, HeroSection, GuestPersonalSpace are client components only because they fetch /api/settings
   489→   489→FILE:   src/components/Footer.tsx:1   `'use client'`
   490→   490→        src/components/GuestSearch.tsx:1   `'use client'`
   491→   491→        src/components/HeroSection.tsx    (uses useState for settings)
   492→   492→PROOF:  All 3 use `'use client'` + `useEffect(() => fetch('/api/settings'))` to read couple
   493→   493→        names + hashtag + photos. The fetch could be done server-side (the wedding slug is
   494→   494→        available in the URL or layout), the result passed as props. Footer in particular
   495→   495→        has zero interactivity — pure rendering of settings data.
   496→   496→IMPACT: Extra round-trip per page load (3-4 fetches per homepage). Client-side state for
   497→   497→        data that doesn't change after first render. Hydration mismatch risk (P1-UX-7 pattern).
   498→   498→FIX:    Convert Footer to a Server Component that takes `settings` as a prop. Pass settings
   499→   499→        from the page server component. GuestSearch and HeroSection may need to stay client
   500→   500→        (they have interactivity), but they can receive initial settings as props and only
   501→   501→        re-fetch on demand.
   502→   502→EFFORT: M
   503→   503→```
   504→   504→
   505→   505→```
   506→   506→P2-PERF-18: Missing loading.tsx for nested routes (only root exists)
   507→   507→FILE:   src/app/loading.tsx (root only — added by P1-UX-1)
   508→   508→        Missing: src/app/platform/loading.tsx, src/app/platform/admin/loading.tsx,
   509→   509→        src/app/w/[slug]/loading.tsx, src/app/w/[slug]/admin/loading.tsx,
   510→   510→        src/app/onboarding/loading.tsx
   511→   511→PROOF:  `find src/app -name "loading.tsx"` returns only the root file. Next.js App Router
   512→   512→        supports per-segment loading.tsx — without them, transitions to nested routes show
   513→   513→        the parent's loading state (or nothing) until the new segment loads.
   514→   514→IMPACT: Transitioning from /platform/admin#/dashboard to /platform/admin#/users (tab switch
   515→   515→        in client) is instant, but navigating from /platform/login → /platform/admin shows
   516→   516→        the root loading.tsx (which is the homepage skeleton — wrong UX).
   517→   517→        **DUPLICATE of P1-UX-1 (partially addressed)**.
   518→   518→FIX:    Add a generic loading.tsx to each of the 5 nested route segments above. Can be a
   519→   519→        simple centered Spinner.
   520→   520→EFFORT: S
   521→   521→```
   522→   522→
   523→   523→### Summary tables
   524→   524→
   525→   525→#### P2-SEC (14 issues)
   526→   526→
   527→   527→| ID | Title | File | Effort |
   528→   528→|----|-------|------|--------|
   529→   529→| P2-SEC-1 | console.error leaks Error.stack (2 routes) | src/app/api/guest/{auto-auth:157,invite:133}/route.ts | S |
   530→   530→| P2-SEC-2 | EMAIL_REGEX duplicated 4× post-P1-CQ-10 | src/app/api/{platform/users:113,onboarding/create-wedding:68,onboarding/leads:23}/route.ts | S |
   531→   531→| P2-SEC-3 | VALID_PLANS / VALID_ROLES duplicated 3× post-P1-CQ-11/12 | src/app/api/{platform/weddings:27,platform/weddings/[id]:33,admin/users:22,platform/users:105}/route.ts | S |
   532→   532→| P2-SEC-4 | Cookies use SameSite=Lax not Strict (4 sites) | src/lib/auth.ts:267 + 3 guest routes | S |
   533→   533→| P2-SEC-5 | Invitation token in URL query param | src/app/api/guest/invite/route.ts:28 | M |
   534→   534→| P2-SEC-6 | No rate limit on 44 of 50 API routes | src/app/api/** (all except 6 login/auth routes) | M |
   535→   535→| P2-SEC-7 | Unsafe JSON.parse(theme.customizations) | src/app/api/theme/route.ts:22,117 | S |
   536→   536→| P2-SEC-8 | Health endpoint leaks DB error.message | src/app/api/health/route.ts:34 | S |
   537→   537→| P2-SEC-9 | Hardcoded dev-fallback secrets in source | src/lib/{auth:40,guest-auth:42,69}.ts | S |
   538→   538→| P2-SEC-10 | bcrypt hash inside db.$transaction | src/app/api/onboarding/create-wedding/route.ts:399 | S |
   539→   539→| P2-SEC-11 | Host header injection in QR code URL | src/app/api/guests/qrcode/[code]/route.ts:84-85 | S |
   540→   540→| P2-SEC-12 | Lookup-token replay window (5min) | src/app/api/guest/auto-auth/route.ts:16-17,79-90 | S |
   541→   541→| P2-SEC-13 | Body token in admin login response (DUPLICATE of P1-SEC-3) | src/app/api/admin/login/route.ts:67-76 | M |
   542→   542→| P2-SEC-14 | auditLog.create never populates ipAddress/userAgent (P1-SEC-12 incomplete) | 40+ call sites across src/app/api/** | M |
   543→   543→
   544→   544→#### P2-CQ (22 issues)
   545→   545→
   546→   546→| ID | Title | File | Effort |
   547→   547→|----|-------|------|--------|
   548→   548→| P2-CQ-1 | EMAIL/PLANS/ROLES dedup incomplete (DUPLICATE of P1-CQ-10/11/12) | (see P2-SEC-2/3) | S |
   549→   549→| P2-CQ-2 | MAX_PAYMENT_USD_CENTS magic number duplicated 3× (DUPLICATE of P1-CQ-19) | src/app/api/{platform/weddings/[id]/subscription:170,platform/weddings/[id]/invoices:145,onboarding/create-wedding:204}/route.ts | S |
   550→   550→| P2-CQ-3 | formatDate / formatUsd / formatFcfa duplicated 3-4× (DUPLICATE of P1-CQ-13) | src/app/platform/admin/{page:377,BillingTab:238,OnboardingTab:344}.tsx | S |
   551→   551→| P2-CQ-4 | PLANS array duplicated 3× in admin UI | src/app/platform/admin/{page:237,BillingTab:205,OnboardingTab:230}.tsx | S |
   552→   552→| P2-CQ-5 | Inconsistent error message language (EN vs FR, period vs no period) | src/app/api/** (50 routes) | M |
   553→   553→| P2-CQ-6 | Inconsistent body parsing — 8 routes use .catch, 22+ use raw .json() | src/app/api/** (22+ routes) | M |
   554→   554→| P2-CQ-7 | auditLog.create never populates ipAddress/userAgent (DUPLICATE of P2-SEC-14) | 40+ call sites | M |
   555→   555→| P2-CQ-8 | Inconsistent route design — PUT/DELETE read id from body or query | src/app/api/{guests,couple-story,tables,timeline}/route.ts | M |
   556→   556→| P2-CQ-9 | Magic strings for Settings keys (bride_name, etc.) | src/app/api/onboarding/create-wedding:365-388 + 4 UI files | S |
   557→   557→| P2-CQ-10 | createWedding POST handler is 453 lines (DUPLICATE of P1-CQ-7) | src/app/api/onboarding/create-wedding/route.ts:93-545 | L |
   558→   558→| P2-CQ-11 | duplicateWedding POST handler is 200 lines (DUPLICATE of P1-CQ-7) | src/app/api/platform/weddings/[id]/duplicate/route.ts:29-234 | M |
   559→   559→| P2-CQ-12 | 4 admin-UI components exceed 500 lines (DUPLICATE of P1-CQ-8 + P1-UX-9) | page.tsx (2452), OnboardingTab (2150), BillingTab (1202), GuestManager (1099) | L |
   560→   560→| P2-CQ-13 | page.tsx duplicates WEDDING_STATUSES + PLANS + STATUS_LABELS from lib/ | src/app/platform/admin/page.tsx:236-268 | S |
   561→   561→| P2-CQ-14 | ROLE_LABELS duplicated between lib/auth.ts and platform/admin/page.tsx | src/lib/auth.ts:284 + src/app/platform/admin/page.tsx:102 | S |
   562→   562→| P2-CQ-15 | Dead code — /api/route.ts returns "Hello, world!" | src/app/api/route.ts (6 lines) | S |
   563→   563→| P2-CQ-16 | Platform dashboard route runs 16 parallel Prisma queries | src/app/api/platform/dashboard/route.ts:85-189 | M |
   564→   564→| P2-CQ-17 | Guest access-logs route runs 19 Prisma queries per request | src/app/api/guest/access-logs/route.ts:32-99 | M |
   565→   565→| P2-CQ-18 | Missing JSDoc on ~44 of 50 API route handlers | src/app/api/** (44 routes without JSDoc) | M |
   566→   566→| P2-CQ-19 | useAuthedFetch pattern duplicated in 4 admin client components | src/app/platform/admin/page.tsx:291-347 + 3 other sites | M |
   567→   567→| P2-CQ-20 | Long inline design-decision comments (no formal markers) | src/app/api/onboarding/create-wedding:341-345 + others | S |
   568→   568→| P2-CQ-21 | setAuthCookie helper exists but used in only 1 of 5 cookie sites | src/lib/auth.ts:263 + 4 inline cookie sets | S |
   569→   569→| P2-CQ-22 | Dashboard route couples 6 analytics sections in one 250-line handler | src/app/api/platform/dashboard/route.ts:67-313 | M |
   570→   570→
   571→   571→#### P2-PERF (18 issues)
   572→   572→
   573→   573→| ID | Title | File | Effort |
   574→   574→|----|-------|------|--------|
   575→   575→| P2-PERF-1 | N+1 in guests/import-docx (up to 10 findFirst per guest) | src/app/api/guests/import-docx/route.ts:278-389 | M |
   576→   576→| P2-PERF-2 | N+1 in guests/import (sequential create per row) | src/app/api/guests/import/route.ts:41-79 | S |
   577→   577→| P2-PERF-3 | Missing pagination on guests/export (fetches ALL guests) | src/app/api/guests/export/route.ts:22-25 | M |
   578→   578→| P2-PERF-4 | Missing pagination on couple-story / tables / timeline (no take) | src/app/api/{couple-story:9,tables:17,timeline:10}/route.ts | S |
   579→   579→| P2-PERF-5 | bcrypt hash inside db.$transaction (DUPLICATE of P2-SEC-10) | src/app/api/onboarding/create-wedding/route.ts:399 | S |
   580→   580→| P2-PERF-6 | Platform dashboard fetches ALL published weddings for MRR | src/app/api/platform/dashboard/route.ts:160-163 | S |
   581→   581→| P2-PERF-7 | Admin dashboard runs 9 sequential queries (3 Promise.all groups) | src/app/api/admin/dashboard/route.ts:21-49 | S |
   582→   582→| P2-PERF-8 | Guest access-logs route runs 19 Prisma queries (DUPLICATE of P2-CQ-17) | src/app/api/guest/access-logs/route.ts:32-99 | M |
   583→   583→| P2-PERF-9 | /api/route.ts uses force-dynamic for static "Hello, world!" | src/app/api/route.ts:1 | S |
   584→   584→| P2-PERF-10 | All 50 API routes use force-dynamic — 4 public reads could be ISR | src/app/api/{couple-story,tables,timeline,settings,theme}/route.ts | M |
   585→   585→| P2-PERF-11 | (NON-ISSUE) Guest.invitationCode unindexed — actually uses composite unique | prisma/schema.prisma:199 | - |
   586→   586→| P2-PERF-12 | (NON-ISSUE) AdminUser.email already indexed via @unique | prisma/schema.prisma:71 | - |
   587→   587→| P2-PERF-13 | Platform admin eagerly imports 7 large tab components (DUPLICATE of P1-UX-9) | src/app/platform/admin/page.tsx:93-97 | M |
   588→   588→| P2-PERF-14 | All list endpoints use offset pagination (no cursor) | src/app/api/{guests,platform/weddings,platform/users,onboarding/leads,guest/access-logs}/route.ts | M |
   589→   589→| P2-PERF-15 | Module-scope setInterval never cleared (DUPLICATE of P1-CQ-20) | src/lib/guest-auth.ts:202 + src/app/api/guest/auto-auth/route.ts:17 | S |
   590→   590→| P2-PERF-16 | Missing Suspense around individual data fetches (DUPLICATE of P1-UX-1) | src/app/{page,w/[slug]/page}.tsx + components | M |
   591→   591→| P2-PERF-17 | Footer/GuestSearch/HeroSection are client components only for settings fetch | src/components/{Footer,GuestSearch,HeroSection}.tsx | M |
   592→   592→| P2-PERF-18 | Missing loading.tsx for 5 nested routes (DUPLICATE of P1-UX-1) | (missing) src/app/{platform,platform/admin,w/[slug],w/[slug]/admin,onboarding}/loading.tsx | S |
   593→   593→
   594→   594→### Notes on duplicates / out-of-scope items
   595→   595→
   596→   596→- **P2-SEC-13 / P2-CQ-1 / P2-CQ-2 / P2-CQ-3 / P2-CQ-10 / P2-CQ-11 / P2-CQ-12 / P2-PERF-13 / P2-PERF-15 / P2-PERF-16 / P2-PERF-18** — these are explicitly marked as DUPLICATEs of open P1 items. The P1-LAUNCH worklog summary listed P1-SEC-3, P1-CQ-1-20, P1-UX-4-10 as "remaining P1 (non bloquants pour commercialisation, à traiter en P2/P3)". This audit enumerates the P2 angles of those P1 items so they can be batched together when the team tackles them.
   597→   597→- **P2-PERF-11 / P2-PERF-12** — explicitly marked NON-ISSUE (the schema already has the indexes; documented for awareness so future devs don't re-add indexes).
   598→   598→- **P2-CQ-20** — informational only, no action needed.
   599→   599→- **Inconsistently-applied P1 fixes** — P1-CQ-10 (EMAIL_REGEX), P1-CQ-11 (VALID_PLANS), P1-CQ-12 (VALID_ROLES), P1-CQ-13 (formatDate), P1-CQ-19 (MAX_PAYMENT) are all marked as "fixed" in the P1-LAUNCH summary but the worklog only documents updating 1 site per constant (admin/users route). The other 3-6 sites still have local copies. This audit's P2-SEC-2/3 + P2-CQ-1/2/3 enumerate those remaining sites.